@model IEnumerable<Ellemy.CQRS.Example.Query.MessageReadModel>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>
@Html.Action("Create")
<ul>
@foreach (var item in Model)
{
   <li>@Html.DisplayFor(m => item.Text)</li>
}
</ul>
